program = expression+

digit = [0-9]
letter = [a-zA-Z]
special_char = [_-=\$%&!\?\*\+\/\\><^]
integer = digit+
float = integer "." integer
identifier = [letter | _ | \$][letter | integer | special_char]*

call = "(" identifier expr+ ")"

expr = integer | float | identifier | call
